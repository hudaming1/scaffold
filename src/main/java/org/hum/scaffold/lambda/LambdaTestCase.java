package org.hum.scaffold.lambda;


public class LambdaTestCase {

	// return locationMaterialMap.values().stream().flatMap(Collection::stream).map(MaterialOrBatchDTO::getMaterialId).collect(Collectors.toList());
}
